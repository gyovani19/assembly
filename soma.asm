.text

    li $t0, 30
    li $t1, 15
    add $s0, $t0, $t1 #soma com registradores
    addi $s1, $s0, 15 # soma registradores com inteiros
    addi $s2, $s1, -100 #tbm é possível somar com inteiros negativos